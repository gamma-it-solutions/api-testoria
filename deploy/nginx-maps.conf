# Testoria shared nginx http-context maps (owned by api-testoria).
# Deploy to /etc/nginx/conf.d/00-testoria-maps.conf so it loads in the http{}
# context before any server block. Required by api.vhost.conf's WebSocket
# `Connection` header handling.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
